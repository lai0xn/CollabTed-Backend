model User {
  id             String          @id @default(auto()) @map("_id") @db.ObjectId
  email          String          @unique
  name           String
  password       String
  profilePicture String
  active         Boolean
  isOAuth        Boolean
  userWorkspace  UserWorkspace[]
  workspace      Workspace[]
  Message        Message[]
}
