datasource db {
  provider = "mongodb"
  url      = env("MONGO_URIi")
}

generator db {
  provider        = "go run github.com/steebchen/prisma-client-go"
  previewFeatures = ["prismaSchemaFolder"]
}
